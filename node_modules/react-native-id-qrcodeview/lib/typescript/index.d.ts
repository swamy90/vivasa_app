import { ViewProps } from 'react-native';
export interface QRCodeProps extends ViewProps {
    qrCodeValue: string;
    backgroundColor?: string;
}
export declare const QRCodeView: ({ qrCodeValue, backgroundColor, ...restProps }: QRCodeProps) => JSX.Element;
